# Backend Configuration

# API Settings
API_TITLE=Portfolio API
API_VERSION=1.0.0

# CORS Settings
CORS_ORIGINS=["http://localhost:8000", "http://localhost:3000", "http://localhost:5173"]

# Server Settings
HOST=0.0.0.0
PORT=8000

# Email Settings (Optional - for future email integration)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SENDER_EMAIL=your-email@gmail.com
# SENDER_PASSWORD=your-app-password

# Database Settings (Optional - for future database integration)
# DATABASE_URL=postgresql://user:password@localhost/portfolio_db
